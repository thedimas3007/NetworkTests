package thedimas.network.enums;

@SuppressWarnings("unused")
public enum DcReason {
    CONNECTION_CLOSED,
    DISCONNECTED,
    SERVER_CLOSED,
    STREAM_CORRUPTED,
    CLASS_NOT_FOUND,
    ACCESS_DENIED,
    TIMEOUT
}
